# This config file is used by ktlint (and potentially other tools)
[*.{kt,kts}]
indent_size=2
max_line_length=120

# KtLint options
# NOTE: import-ordering is disabled due to a ktlint issue:
#   https://github.com/pinterest/ktlint/issues/527
#   Re-enable when it gets resolved
disabled_rules=import-ordering
